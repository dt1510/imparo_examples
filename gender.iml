%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% Gender learning problem. 
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

% These parameters can be varied to control the form of the hypotheses learned.
:-set_max_clause_length(60).
:-set_max_clauses(10).
:-set_connected(1).
:-set_max_var_depth(40).

head_modes([
    woman(+person)
]).

body_modes([
    female(+person),
    male(+person)
]).

person(bob).
person(tom).
person(john).
person(david).
person(terry).

person(ann).
person(mary).
person(susan).
person(jane).
